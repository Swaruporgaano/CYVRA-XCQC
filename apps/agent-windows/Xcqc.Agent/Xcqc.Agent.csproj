<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Xcqc.Agent</RootNamespace>
    <AssemblyName>Xcqc.Agent</AssemblyName>
    <Version>0.1.0</Version>
  </PropertyGroup>
  <!-- Prefer: dotnet publish -c Release -r win-x64 -p:SelfContained=true -p:PublishSingleFile=true -->
  <ItemGroup>
    <ProjectReference Include="..\Xcqc.Collectors\Xcqc.Collectors.csproj" />
  </ItemGroup>
</Project>
